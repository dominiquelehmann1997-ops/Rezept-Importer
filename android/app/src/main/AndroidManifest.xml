<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      usesCleartextTraffic: Das Dashboard laeuft im Heimnetz ohne eigenes
      Zertifikat (z.B. http://192.168.178.91:3001). Seit targetSdk 28 blockiert
      Android http:// sonst komplett ("CLEARTEXT communication not permitted") —
      der WLAN-Betrieb waere damit unmoeglich.

      Bewusst pauschal statt per network-security-config: Deren <domain>-Eintraege
      koennen kein CIDR, ein Heimnetz-Bereich muesste also Adresse fuer Adresse
      aufgezaehlt werden (~1000 Eintraege pro /24) und waere nach einem
      Routertausch trotzdem falsch. Die App ist eine privat sideloadbare
      Debug-Build und spricht genau eine Adresse an, die der Nutzer selbst in den
      Einstellungen eintraegt; unterwegs laeuft sie ohnehin ueber HTTPS via
      Cloudflare. Der Gewinn der Aufzaehlung waere theoretisch, die Wartungslast real.
    -->
    <application
        android:name=".ObsiDineApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.RezeptImporter"
        android:allowBackup="false"

        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ShareActivity"
            android:exported="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="de.dml.rezeptimporter.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
